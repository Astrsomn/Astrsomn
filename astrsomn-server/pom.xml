<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.astrsomn</groupId>
        <artifactId>Astrsomn</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>
    <artifactId>astrsomn-server</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>astrsomn-server</name>
    <description>AI分析服务</description>
    <packaging>jar</packaging>

    <properties>
        <java.version>17</java.version>
    </properties>

    <dependencies>
        <!-- 核心模块 -->
        <dependency>
            <groupId>org.astrsomn</groupId>
            <artifactId>astrsomn-spring-boot-starter</artifactId>
        </dependency>

        <dependency>
            <groupId>org.astrsomn</groupId>
            <artifactId>astrsomn-vector</artifactId>
            <type>pom</type>
        </dependency>

        <!-- 各 provider 为独立 jar；聚合 pom(astrsomn-providers) 不会带入子模块，须逐个声明以便 SPI 与 classpath 扫描 -->
        <dependency>
            <groupId>org.astrsomn</groupId>
            <artifactId>astrsomn-provider-deepseek</artifactId>
            <version>${astrsomn.version}</version>
        </dependency>
        <dependency>
            <groupId>org.astrsomn</groupId>
            <artifactId>astrsomn-provider-qwen</artifactId>
            <version>${astrsomn.version}</version>
        </dependency>
        <dependency>
            <groupId>org.astrsomn</groupId>
            <artifactId>astrsomn-provider-openai</artifactId>
            <version>${astrsomn.version}</version>
        </dependency>
        <dependency>
            <groupId>org.astrsomn</groupId>
            <artifactId>astrsomn-provider-qianfan</artifactId>
            <version>${astrsomn.version}</version>
        </dependency>
        <dependency>
            <groupId>org.astrsomn</groupId>
            <artifactId>astrsomn-provider-zhipu</artifactId>
            <version>${astrsomn.version}</version>
        </dependency>

        <dependency>
            <groupId>org.astrsomn</groupId>
            <artifactId>astrsomn-workflow-spring-boot-starter</artifactId>
        </dependency>

        <!-- Spring Boot -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-crypto</artifactId>
        </dependency>

        <!-- Spring Cloud -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-openfeign</artifactId>
        </dependency>


        <!-- MyBatis-Plus -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
            <version>3.5.5</version>
        </dependency>

        <!-- 数据库驱动 -->
        <dependency>
            <groupId>com.oracle.database.jdbc</groupId>
            <artifactId>ojdbc11</artifactId>
            <version>21.1.0.0</version>
        </dependency>

        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>



        <!-- 测试 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <repository>
            <id>central</id>
            <name>Maven Central</name>
            <url>https://repo1.maven.org/maven2/</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
    </repositories>
</project>