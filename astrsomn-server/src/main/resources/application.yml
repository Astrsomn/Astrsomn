spring:
  application:
    name: astrsomn-server
  profiles:
    active: h2
  servlet:
    multipart:
      max-file-size: 128MB
      max-request-size: 130MB

server:
  port: 4481

astrsomn:
  env-code: pro

logging:
  level:
    org.astrsomn.core.mapper: debug
