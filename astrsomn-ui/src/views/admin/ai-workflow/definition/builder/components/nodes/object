节点类别,核心节点名称,功能定义,开发者关注点 (以 Java/LangChain4j 为例)
基础/入口,开始 (Start),定义全局入参及变量校验。,强类型定义、输入 Schema 校验。
AI 核心,LLM (大语言模型),调用模型进行推理、对话、格式化输出。,Model Factory 适配、Prompt 模板注入。
知识检索,知识库 (Retrieval),向量数据库检索（RAG）获取背景知识。,Embedding 适配、分段召回策略。
逻辑控制,条件分支 (If/Else),根据变量或表达式结果决定执行路径。,表达式引擎 (如 SpEL 或 Aviator)。
逻辑控制,问题分类器,利用 LLM 自动将意图分类路由。,内部封装的高频 LLM 分类 Prompt。
逻辑控制,并行 (Parallel),同时触发多个下游分支以提升响应速度。,多线程编排、CompletableFuture 处理。
数据加工,代码执行 (Code),运行 Python/NodeJS 脚本处理数据。,沙箱环境隔离、多语言 Runtime 支持。
数据加工,变量聚合 (Merge),将多个分支生成的变量汇聚成统一变量。,变量作用域管理、数据覆盖规则。
外部扩展,HTTP 请求,通过标准 Restful API 调用第三方系统。,连接池管理、超时/重试机制、Auth 认证。
外部扩展,工具 (Tools),调用预定义的原子工具（如查搜索、画图）。,插件化设计、Function Calling 自动映射。
结束/输出,结束 (End),定义工作流的最终输出结构。,统一的 Result 对象、错误码处理。